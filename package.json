{
  "name": "paymeback-fullstack",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": "18.x" },
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "heroku-postbuild": "cd frontend && npm run build",
    "start": "node backend/src/app.js"
  }
}

